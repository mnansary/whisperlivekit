# This is an example environment file.
# 1. Copy this file to a new file named .env
# 2. Fill in the values for your environment.

# --- LiveKit Server Configuration ---
# These are the default keys for a local LiveKit instance.
# For a production deployment, use strong, randomly generated secrets.
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# --- Frontend Client Token ---
# This is a long-lived token that the frontend will use to connect to LiveKit.
# You MUST generate a token and paste it here.
#
# How to generate the token:
# 1. Make sure you have the LiveKit CLI installed: https://docs.livekit.io/cli/
# 2. Run the following command in your terminal:
#
# livekit-cli create-token --api-key devkey --api-secret secret --join --room bangla-voice-agent --identity user-frontend --valid-for 720h
#
# 3. Copy the entire output (the long string starting with 'eyJ...') and paste it below.

LIVEKIT_FRONTEND_TOKEN="<PASTE_YOUR_GENERATED_TOKEN_HERE>"
